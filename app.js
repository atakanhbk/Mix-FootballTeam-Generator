const selectOption = document.getElementsByClassName("playerNumber")[0];


const selectOptionFunction = () => {
  console.log("Test");
};

// Button Click Event Listener

selectOption.addEventListener("change", selectOptionFunction);
